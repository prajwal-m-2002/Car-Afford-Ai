# This file is created as requested.
# Please note: Spring Boot loads both application.yml and application.properties.
# A common best practice is to choose ONE format to avoid overriding configurations.
# We have configured your TiDB database connection in application.yml.
# If you prefer properties format, you can uncomment and use the below lines instead of application.yml.

# spring.application.name=CarAfford AI
# server.port=8080

# spring.datasource.url=jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/carafford_db?sslMode=VERIFY_IDENTITY&tlsVersions=TLSv1.2,TLSv1.3
# spring.datasource.username=2BZjwuhbXKLyStx.root
# spring.datasource.password=79D8mHrtVNkR073e
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# spring.jpa.properties.hibernate.format_sql=true

# Uncomment the below lines if you want schema.sql & data.sql to initialize your database
# spring.sql.init.mode=always
